
<wxs src="./Fragment.wxs" module="helper" />
<template name="level1">
<block wx:for="{{nodes}}" wx:key="id">
	<block wx:if="{{item.tag === 'fragment'}}"><Fragment id="{{item.id}}" nodes="{{item.children}}"/></block>
	<view wx:elif="{{item.tag === 'view'}}" id="{{item['id']}}" style="{{item['style']}}" catch:tap="eh"><template is="level2" data="{{ nodes: item.children }}" /></view>
	<swiper wx:elif="{{item.tag === 'swiper'}}" indicator-dots="{{item['indicator-dots']}}" indicator-color="{{item['indicator-color']}}"><template is="level2" data="{{ nodes: item.children }}" /></swiper>
	<swiper-item wx:elif="{{item.tag === 'swiper-item'}}" ><template is="level2" data="{{ nodes: item.children }}" /></swiper-item>
	<block wx:elif="{{helper.isString(item)}}">{{item}}</block>
	<template wx:elif="{{item.tag}}" is="{{item.tag}}" data="{{...item}}" />
</block>
</template>
<template name="level2">
<block wx:for="{{nodes}}" wx:key="id">
	<block wx:if="{{item.tag === 'fragment'}}"><Fragment id="{{item.id}}" nodes="{{item.children}}"/></block>
	<view wx:elif="{{item.tag === 'view'}}" id="{{item['id']}}" style="{{item['style']}}" catch:tap="eh"><template is="level3" data="{{ nodes: item.children }}" /></view>
	<swiper wx:elif="{{item.tag === 'swiper'}}" indicator-dots="{{item['indicator-dots']}}" indicator-color="{{item['indicator-color']}}"><template is="level3" data="{{ nodes: item.children }}" /></swiper>
	<swiper-item wx:elif="{{item.tag === 'swiper-item'}}" ><template is="level3" data="{{ nodes: item.children }}" /></swiper-item>
	<block wx:elif="{{helper.isString(item)}}">{{item}}</block>
	<template wx:elif="{{item.tag}}" is="{{item.tag}}" data="{{...item}}" />
</block>
</template>
<template name="level3">
<block wx:for="{{nodes}}" wx:key="id">
	<block wx:if="{{item.tag === 'fragment'}}"><Fragment id="{{item.id}}" nodes="{{item.children}}"/></block>
	<view wx:elif="{{item.tag === 'view'}}" id="{{item['id']}}" style="{{item['style']}}" catch:tap="eh"><template is="level4" data="{{ nodes: item.children }}" /></view>
	<swiper wx:elif="{{item.tag === 'swiper'}}" indicator-dots="{{item['indicator-dots']}}" indicator-color="{{item['indicator-color']}}"><template is="level4" data="{{ nodes: item.children }}" /></swiper>
	<swiper-item wx:elif="{{item.tag === 'swiper-item'}}" ><template is="level4" data="{{ nodes: item.children }}" /></swiper-item>
	<block wx:elif="{{helper.isString(item)}}">{{item}}</block>
	<template wx:elif="{{item.tag}}" is="{{item.tag}}" data="{{...item}}" />
</block>
</template>
<template name="level4">
<block wx:for="{{nodes}}" wx:key="id">
	<block wx:if="{{item.tag === 'fragment'}}"><Fragment id="{{item.id}}" nodes="{{item.children}}"/></block>
	<view wx:elif="{{item.tag === 'view'}}" id="{{item['id']}}" style="{{item['style']}}" catch:tap="eh"><template is="level5" data="{{ nodes: item.children }}" /></view>
	<swiper wx:elif="{{item.tag === 'swiper'}}" indicator-dots="{{item['indicator-dots']}}" indicator-color="{{item['indicator-color']}}"><template is="level5" data="{{ nodes: item.children }}" /></swiper>
	<swiper-item wx:elif="{{item.tag === 'swiper-item'}}" ><template is="level5" data="{{ nodes: item.children }}" /></swiper-item>
	<block wx:elif="{{helper.isString(item)}}">{{item}}</block>
	<template wx:elif="{{item.tag}}" is="{{item.tag}}" data="{{...item}}" />
</block>
</template>
<template name="level5">
	<Fragment nodes="{{nodes}}"/>
</template>
<template name="image">
	<image src="{{src}}" catch:load="eh" catch:tap="eh"/>
</template>
<template is="level1" data="{{ nodes: nodes }}"/>
